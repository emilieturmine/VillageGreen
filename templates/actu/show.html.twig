{% extends 'base.html.twig' %}

{% block title %}Actu
{% endblock %}

{% block body %}
	
	<div class="col-12">
		<h1 class="text-info text-center mt-5">Actualités</h1>
	</div>
	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ actu.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ actu.nom }}</td>
			</tr>
			<tr>
				<th>Type</th>
				<td>{{ actu.type }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ actu.date ? actu.date|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Lieu</th>
				<td>{{ actu.lieu }}</td>
			</tr>
			<tr>
				<th>Photo1</th>
				<td>{{ absolute_url ("{{ actu.photo1 }}")}}</td>
			</tr>
			<tr>
				<th>Photo2</th>
				<td>"{{ absolute_url ("{{ actu.photo2 }}")}}"</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_actu_index') }}">back to list</a>

	<a href="{{ path('app_actu_edit', {'id': actu.id}) }}">edit</a>

	{{ include('actu/_delete_form.html.twig') }}
{% endblock %}
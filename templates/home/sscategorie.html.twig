{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
		<h5></strong>Catégorie  {{ sscategorie.catParent.nom }} Sous categorie {{ sscategorie.nom }} </strong></h5>
		{% for prosscat in sscategorie.produits %}
			
			<div class="col-3"><a href="{{ path("app_home_produit", {produit: prosscat.id}) }}">
				<div class="card">
					<div class="card-body">
						<img src="/imagesVG/propho/{{prosscat.photo}}"alt="card-img-top">
						<h6 class="card-title">{{prosscat.libelle}}</h6>
					</div>
					</a>
				</div>
			</div>
			
		{% endfor %}
		<p></p>
	
</article>{% endblock %}

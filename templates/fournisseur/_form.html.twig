<div class="text-left mt-5 mx-5 my-5 h5">
{{ form_start(form) }}

    {{ form_row(form.nom,{
        label: 'Nom:', attr: {
        placeholder:'Le nom du fournisseur'}
    }) }}

    {{ form_row(form.photo,{
        label: 'Photo:', attr: {
        placeholder:'Le nom du fichier dans le dossier imagesVG/foupho'}
    }) }}



    {{ form_row(form.adresse,{
        label: 'Adresse:', attr: {
        placeholder:"Son adresse "}
    }) }}

    {{ form_row(form.cp,{
        label: 'Code Postal:', attr: {
        placeholder:'Son code postal'}
    }) }}

    {{ form_row(form.ville,{
        label: 'Ville:', attr: {
        placeholder:'La ville du fournisseur'}
    }) }}

</div>    

    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>

   {{ form_end(form) }}

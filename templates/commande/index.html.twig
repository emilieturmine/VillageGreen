{% extends 'base.html.twig' %}

{% block title %}Liste des Commandes{% endblock %}
{% block body %}

<div class="col-12">


    <h1 class="text-info text-center mt-5">Liste des Commandes</h1>
</div>
<div id="crud" class="col-12 text-center   mt-5">
    <table class="table ">
        <thead class="table bg-info mt-5 text-white text-center">
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Statut</th>
                <th>Total</th>
                <th>Id client</th>
                <th>Détails</th>
            </tr>
        </thead>
        <tbody class="text-center">
        {% for commande in commandes %}
            <tr>
                <td>{{ commande.id }}</td>
                <td>{{ commande.date ? commande.date|date('d-m-Y') : '' }}</td>
                <td>{{ commande.statut }}</td>
                <td>{{ commande.total }}</td>
                <td>{{ commande.user.id }}</td>
                <td><a href="{{ path('app_commande_show', {'id': commande.id}) }}">Voir</a></td>
                
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
   
        </tbody>
    </table>
 </div>

   
{% endblock %}
